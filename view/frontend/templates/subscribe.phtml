<?php
//Don't care if form collection is disabled as bots would get added also with sync only.
$isSmailyEnabled = $block->isSmailyEnabled();

if ($isSmailyEnabled) {
    echo $block->getTemplateWithCaptcha();
} else {
    echo $block->originalTemplateHtml();
}
