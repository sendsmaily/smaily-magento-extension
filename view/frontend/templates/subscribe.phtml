<?php
$newsletterSubscriptionEnabled = $block->isNewsletterSubscriptionEnabled();
$captchaEnabled = $block->isCaptchaEnabled();
// TODO : Better way to render magento CAPTCHA.
if ($newsletterSubscriptionEnabled && $captchaEnabled) {
    echo $block->getTemplateWithCaptcha();
} else {
    echo $block->originalTemplateHtml();
}
